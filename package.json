{
	"private": true,
	"name": "novel-archives",
	"scripts": {
		"lint:prettier": "prettier --check",
		"fix:prettier": "npm run lint:prettier -- --write",
		"postinstall": "husky install"
	},
	"devDependencies": {
		"husky": "^5.0.9",
		"lint-staged": "^13.2.1",
		"prettier": "^2.2.1"
	}
}
